name: Build

jobs:
    build:
        runs-on: windows-latest
        steps:
            - uses: actions/checkout@v3
            - uses: actions/setup-dotnet@v1
            - name: Build
            run: dotnet build
            - name: Run tests
            run: dotnet test
